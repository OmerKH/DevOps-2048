apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "2048-chart.fullname" . }}-test-connection"
  labels:
    {{- include "2048-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "2048-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
